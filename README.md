# Un ciel bleu pour Poitiers

## Génération du CSS

- `npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch`

## Test sur smartphone

- Récupérer l'IP du PC `ipconfig`
- Démarrer un serveur local `python -m http.server 8000`
